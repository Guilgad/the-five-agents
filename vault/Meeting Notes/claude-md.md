# CLAUDE.md

## Overview
קובץ ההוראות הראשי של ה-AI הראשי (ראובן). הקובץ מגדיר את זהות ראובן כמנכ"ל הצוות, את שלושת הסוכנים שתחתיו (יעל, יובל, חן), ואת מבנה התיקיות של הפרויקט. זהו ה"חוקה" של המערכת — ראובן מקרא אותו בכל סשן. הקובץ הוא ראשוני בלבד ועתיד להתרחב עם הוראות ניתוב מדויקות.

**מיקום בפרויקט:** `/CLAUDE.md`
**שייך ל:** ראובן (מנכ"ל) — הוא ה-AI שקורא ומבצע את ההוראות בקובץ זה.

## Open Questions
- none

## Session Log

### 2026-05-22 — תיעוד ראשוני [planned]
- **What was done:** קריאה וסיכום של CLAUDE.md הראשוני.
- **Decisions:** הקובץ מכיל תשתית בלבד — אין עדיין הוראות ניתוב ספציפיות.
- **Notes / Caveats:** הקובץ מציין שהוא "ראשוני" ויתרחב; יש לעדכן תיק זה עם כל שינוי ב-CLAUDE.md.
- **Related:** [[team-personas]], [[agents-directory]], [[project-file-mapping]]

### 2026-05-23 — הוספת ניתוב יובל + section "פייפליין מאמר עם תמונות" [shipped]
- **What was done:** עודכן `CLAUDE.md` בשלושה מקומות: (1) בלוק "ניתוב לסוכנים" — הוחלפה השורה `יובל — TODO` בבלוק מלא עם trigger keywords עברית+אנגלית (תמונה של/ציור של/תיצור תמונה/איור + image of/picture of/generate image/illustration/draw), קלט/פלט, תלות ב-`OPENAI_API_KEY` וב-[[gpt-image-gen-skill]]. (2) section חדש מתחת לבלוק הניתוב — "פייפליין: מאמר עם תמונות (יעל ↔ יובל)" עם 5 שלבים (a–e): יעל כותבת ומשאירה `{{IMAGE_NEEDED: ...}}` placeholders → ראובן מפעיל את יובל על כל placeholder → ראובן משלב את התמונות שחזרו לקבצי ה-MD/HTML של יעל → שומר ב-`Output/`. (3) בלוק "מבנה התיקיות" — הוספת השורה על `yuval/` (reference + outputs).
- **Decisions:** הפייפליין מתועד ב-CLAUDE.md עצמו (ולא רק בקבצי הסוכנים) כי ראובן הוא המתאם בין הסוכנים — זה התפקיד שלו. הסוכנים עצמם לא יודעים אחד על השני; רק על הפורמט המשותף (placeholder).
- **Notes / Caveats:** השאריות של "TODO" נשארות רק ל-חן עכשיו. כשחן תיווצר, תידרש פעימה דומה: עדכון הניתוב + תיק נושא + index.
- **Related:** [[yuval-agent]], [[gpt-image-gen-skill]], [[yael-agent]], [[agents-directory]]

### 2026-05-23 — הוספת ניתוב חן + pipeline מאמר מהרשת [shipped]
- **What was done:** עודכן `CLAUDE.md` בארבעה מקומות: (1) רשימת הצוות — חן עודכנה מתיאור כללי לתיאור מדויק עם WebSearch/WebFetch; (2) בלוק "ניתוב לסוכנים" — הוחלפה שורת TODO של חן בבלוק מלא עם trigger keywords עברית+אנגלית (חפש/מצא/מחקר/מאמר על... + search/find/research...), קלט/פלט, וטווח; (3) section חדש "פייפליין: מאמר מהרשת (חן → יעל → יובל)" עם 4 שלבים (a-d) כולל התפצלות בין "מצא בלבד" ל-"מצא + שכתב"; (4) בלוק "מבנה התיקיות" — הוספת `chen/` עם `Memory/searches.md`. גם הוסרה שורת "הערה ראשוני" שהפכה לא-רלוונטית.
- **Decisions:** ה-pipeline החדש מתועד ב-CLAUDE.md (לא רק בקובץ הסוכן) כי ראובן הוא המתאם — עקבי עם ה-pipeline של יעל↔יובל. ההחלטה על נקודת ה-split ("מצא בלבד" vs "שכתב גם") נשארת אצל ראובן ולא מאוטמטת בתוך חן.
- **Notes / Caveats:** CLAUDE.md כעת מלא — כל שלושת הסוכנים מוגדרים עם routing מפורש.
- **Related:** [[chen-agent]], [[agents-directory]], [[yael-agent]], [[yuval-agent]]
